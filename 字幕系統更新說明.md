# 字幕系統更新說明 - 完全基於 Notify 事件

## 變更概述

根據您的要求，我已經完全移除了前端基於時間計算的字幕顯示邏輯，改為完全依賴後端的 notify 事件來驅動字幕顯示。這確保了字幕與實際的 TTS 語音完全同步。

## 主要修改

### 1. 後端修改 (`basereal.py`)

**新增方法：**
```python
def get_notify_events(self):
    """獲取所有待處理的 notify 事件"""
    events = []
    try:
        while not self.notify_events.empty():
            events.append(self.notify_events.get(block=False))
    except:
        pass
    return events
```

### 2. 前端修改 (`rtcpushapi_google_split4.html`)

#### 2.1 簡化的字幕顯示函數
```javascript
function showSubtitle(text) {
    const aiResponseDisplay = document.getElementById('aiResponseDisplay');
    if (aiResponseDisplay) {
        // 清除可能存在的淡出定時器
        if (window.subtitleFadeTimer) {
            clearTimeout(window.subtitleFadeTimer);
        }
        
        // 直接顯示字幕，不設定自動隱藏
        aiResponseDisplay.textContent = text;
        aiResponseDisplay.style.opacity = '1';
        aiResponseDisplay.style.transition = 'opacity 0.3s ease-in-out';
        
        console.log('Showing subtitle:', text);
    }
}
```

#### 2.2 移除的邏輯

**在 `transcribeAudio` 函數中移除：**
- ✅ 立即顯示第一句的邏輯
- ✅ 動態計算每句話顯示時間的邏輯
- ✅ `switchToNextSentence` 函數
- ✅ `setTimeout` 相關的時間計算

**在 `handleTextSubmit` 函數中移除：**
- ✅ 立即顯示第一句的邏輯
- ✅ 動態計算每句話顯示時間的邏輯
- ✅ `switchToNextSentence` 函數
- ✅ `setTimeout` 相關的時間計算

**在 `handlePresetQuestion` 函數中移除：**
- ✅ 立即顯示第一句的邏輯
- ✅ 動態計算每句話顯示時間的邏輯
- ✅ `switchToNextSentence` 函數
- ✅ `setTimeout` 相關的時間計算

**在 `handleQuickIntro` 函數中移除：**
- ✅ 立即顯示第一句的邏輯
- ✅ 動態計算每句話顯示時間的邏輯
- ✅ `switchToNextSentence` 函數
- ✅ `setTimeout` 相關的時間計算

#### 2.3 保留的邏輯

**所有函數都保留：**
- 分割 AI 回應為多句話的邏輯
- 發送所有句子給虛擬人的邏輯
- 對話歷史記錄的邏輯
- 錯誤處理邏輯

## 新的工作流程

1. **用戶發送訊息** → 前端調用 AI 服務獲取回應
2. **分割回應** → 將完整回應分割為多個句子
3. **發送給虛擬人** → 所有句子依序發送給後端 `/human` API
4. **後端處理** → TTS 開始處理每個句子時，觸發 `notify` 事件
5. **前端輪詢** → 每 300ms 查詢 `/get_notify_events` API
6. **即時顯示** → 收到 `status: 'start'` 事件時立即顯示對應文字

## 優勢

1. **完全同步**：字幕與 TTS 語音完全同步，沒有時間差
2. **簡化邏輯**：移除了複雜的時間計算邏輯，程式碼更簡潔
3. **即時響應**：300ms 輪詢確保字幕能夠即時顯示
4. **統一機制**：所有對話場景（語音輸入、文字輸入、預設問題、快速介紹）都使用相同的字幕機制

## 測試建議

1. **語音輸入測試**：說話後觀察字幕是否與虛擬人說話同步
2. **文字輸入測試**：輸入文字後觀察字幕顯示
3. **預設問題測試**：點擊預設問題按鈕後觀察字幕
4. **快速介紹測試**：點擊快速介紹按鈕後觀察字幕

現在字幕系統完全依賴後端的實際 TTS 事件，確保了最佳的同步效果。
